# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db

# Firebase CLI 本地缓存
.firebase/
firebase-debug.log
firebase-debug.*.log

# Node 依赖（若以后本地安装）
node_modules/

# 环境变量与本地密钥（切勿提交）
.env
.env.*
*-firebase-adminsdk-*.json
serviceAccountKey.json

# 编辑器 / IDE 本地配置
.vscode/
.idea/
# .cursor/ 整体忽略，但保留共享的自定义 Slash 命令
.cursor/*
!.cursor/commands/
*.swp
*.swo

# 本地测试依赖（playwright 等，非应用运行时依赖）
# package-lock.json 不提交；需要锁定版本时可移除此行并提交 lock 文件
package-lock.json
test-results/
playwright-report/
.pw-browsers/

# .cursor/ 已整体忽略（含 hooks 运行时 .test-* 临时文件）

# 交互稿 / 原型（仅本地评审，不提交、不部署）
prototypes/
